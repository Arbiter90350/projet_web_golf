# Vite (dev) — URL de base de l'API
# Utilisé quand vous lancez `npm run dev` dans `frontend/` avec un backend local (PORT=5000)
VITE_API_URL=http://localhost:5001/api/v1
# Timeout global Axios (ms) pour éviter les requêtes pendantes en dev
VITE_API_TIMEOUT_MS=20000
